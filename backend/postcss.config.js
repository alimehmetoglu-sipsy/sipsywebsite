// PostCSS configuration for Strapi backend
// This file prevents Vite from using the root postcss.config.js (for Next.js)
// Strapi admin panel uses its own internal PostCSS configuration

module.exports = {
  plugins: [],
};
